[project]
name = "starhtml-demo"
version = "0.1.0"
description = "StarHTML Demo Hub"
requires-python = ">=3.12"
dependencies = [
    "starhtml>=0.5.4",
    "starelements>=0.1.1",
    "star-drawing>=0.1.0",
    "starlighter",
    "httpx",
    "starlette",
]

[tool.uv.sources]
starhtml = { path = "..", editable = true }
starelements = { path = "../../starelements", editable = true }