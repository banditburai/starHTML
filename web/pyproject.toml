[project]
name = "starhtml-demo"
version = "0.1.0"
description = "StarHTML Demo Hub"
requires-python = ">=3.12"
dependencies = [
    "starhtml>=0.5.3",
    "starlighter",
    "httpx",
    "starlette",
]