{
  "name": "starhtml",
  "version": "0.1.0",
  "description": "StarHTML JavaScript plugins",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun --watch run scripts/build.ts",
    "lint": "biome lint typescript/",
    "format": "biome format typescript/ --write",
    "check": "biome check typescript/",
    "postinstall": "bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.0",
    "@floating-ui/dom": "^1.7.4",
    "typescript": "^5.3.0"
  }
}
